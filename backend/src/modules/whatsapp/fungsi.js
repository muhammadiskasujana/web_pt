// modules/whatsapp/fungsi.js

function cube(kalimat) {
    return `✅ Fungsi test dipanggil dengan kalimat: ${kalimat}`;
}

// contoh fungsi lain
function upper(teks) {
    return teks.toUpperCase();
}

module.exports = { cube, upper };
